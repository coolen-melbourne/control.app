🚀 Installation & Setup
This project is built using Node.js, Express.js for the backend, and Handlebars (HBS) as the view engine for frontend rendering, along with HTML & CSS.
Follow the steps below to run the project locally.
📌 Prerequisites
Make sure you have the following installed on your machine:
Node.js (v18 or higher recommended)
npm (comes with Node.js)
A terminal (macOS Terminal, Git Bash, or similar)
Check versions:
node -v
npm -v
📥 Clone the Repository
git clone https://github.com/your-username/your-project-name.git
cd your-project-name
📦 Install Dependencies
Install all required packages:
npm install
This will install:
express – backend framework
hbs – Handlebars view engine
nodemon – development auto-restart tool
⚙️ Project Structure
project/
├── server.js
├── package.json
├── views/
│   └── home.hbs
├── public/
│   └── css/
│       └── style.css
└── node_modules/
